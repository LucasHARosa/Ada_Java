package mod3.aula1;

public interface Transferenciabancaria {

    void transferir (String contaOrigem, String contaDestino, double valor);

    // Interface de marcação é um recurso utilizado para quando temos interfaces que não possuem métodos ou campos. A im
    // plementação dessa interface em um classe serve para marcar que uma classe pertence à categoria da interface imple
    // mentada e indicar à JVM que aquela classe terá um comportamento de acordo com o contrato definido
    //
}
